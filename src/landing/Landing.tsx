/**
 * The landing page, for native builds.
 *
 * The web page is built on the DOM — GSAP, Framer Motion and CSS — and lives
 * in Landing.web.tsx, which Metro resolves ahead of this file for the web
 * bundle. Native keeps the component-library page it already had.
 */
export { default } from '../screens/LandingScreen';
