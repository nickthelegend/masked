# package-lock.json here was resolved with legacy-peer-deps (the peers of @coral-xyz/anchor and
# stateless.js are not installed); without the same flag a clean `npm ci` reports the lock out of sync.
legacy-peer-deps=true
