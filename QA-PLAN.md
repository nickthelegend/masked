# QA-PLAN — full browser pass, 2026-09-13

Written **before** any item was executed, as the checklist this run is measured
against. It supersedes `TEST-PLAN.md` for this run and reuses its proven items
where the product has not changed. Every item states the specific result that
counts as correct.

## Method

- **What is tested.**
  - The production web export, rebuilt from the current source at 21:34 UTC. It includes the new `src/landing` page.
  - It is served by `npm run serve` on `:4180`.
  - The chain behind it is the local MagicBlock stack, re-created from genesis at 21:34 UTC after the old ledger's base clock drifted 44.4 h behind the rollup:
    - base `mb-test-validator` `:8999`
    - `ephemeral-validator` `:7799`
    - `query-filtering-service` gate `:6699`
    - market proxy `:8791`
  - **Reset again from genesis at 01:11 UTC**, to preload MagicBlock's committor program (Q14). Evidence addresses from before then belong to the ledger kept at `.localnet/ledger-no-committor-20260913T011122Z` (deleted 2026-09-13 20:06 UTC in a disk cleanup, with the owner's OK) (rollup: `.localnet/er-no-committor-20260913T011122Z`) and do not exist on the running chain.
  - **And again at 01:43 UTC**, because the tape-window fix (J9) changes the `Position` and `Tape` layouts. Addresses from 01:11–01:43 belong to `.localnet/ledger-pre-window-20260913T014342Z` (deleted 2026-09-13 20:06 UTC in a disk cleanup, with the owner's OK) (rollup: `.localnet/er-pre-window-20260913T014342Z`).
- **How.**
  - Claude in Chrome, driving the user's real Chrome.
  - Two origins — `127.0.0.1:4180` and `localhost:4180` — give two independent in-page wallets, so two players can duel.
  - On-chain effects are confirmed by reading the chain, not the UI.
- **Pass bar.** An item passes only when all three hold:
  1. the observed result matches **Correct means** exactly;
  2. there are **zero console errors** in that tab during the item (`read_console_messages`, errors only);
  3. there are **zero failed network requests** — no status ≥ 400 and no network error (`read_network_requests`).
- **Browser-extension traffic is not the app.** The Chrome driving this run has wallet extensions that inject scripts into every page. Their requests (`chrome-extension://…`, including a `chrome-extension://invalid/` 503) and their console lines (`[CELL] provider installed…`) are recorded but are outside the tested surface. Every request from the app's own origins and services counts.
- **Fault-injection items.**
  - Marked ⚡.
  - Requests to the one service deliberately stopped are the injected fault and do not fail the item.
  - Anything else that errors does.
- **Status.**
  - **PASS**
  - **FAIL** — the root cause is fixed, then the item is re-run from the start.
  - **UNTESTED** — only when a real external dependency is missing, and it is named.
- **Phase 4.** After every FAIL is fixed, the whole plan is executed again, top to bottom.

---

## A. Environment

| # | Item | Correct means | Status |
|---|---|---|---|
| A1 | Base validator `:8999` | `getHealth` → `ok`; `getSlot` advances between two reads 2 s apart | **PASS** (final run, 02:48 UTC) — `getHealth` → `ok`; `getSlot` 7223 → 7227 across 2 s |
| A2 | Rollup `:7799` | `getHealth` → `ok`; slot advances | **PASS** (final run, 02:48 UTC) — `getHealth` → `ok`; slot 77894 → 77934 across 2 s |
| A3 | Gate `:6699` | `getHealth` → `ok` (refusal behaviour is F4 and Q8) | **PASS** (final run, 02:48 UTC) — `getHealth` → `ok` |
| A4 | Clocks agree | Base and rollup `Clock` sysvar `unix_timestamp` within 60 s of each other and of wall time | **PASS** (final run, 02:49 UTC) — Clock sysvars: base 1789267759, rollup 1789267760, wall 1789267761 — base−rollup −1 s, base−wall −2 s, rollup−wall −1 s |
| A5 | Market proxy `:8791` | `GET /whoami` → exactly `{"service":"masked-market-proxy","upstreams":["/pump/","/jup/"],"imageRelay":"/img?url="}` | **PASS** (final run, 02:48 UTC) — `GET /whoami` → exactly `{"service":"masked-market-proxy","upstreams":["/pump/","/jup/"],"imageRelay":"/img?url="}` |
| A6 | Export server `:4180` | The export server under test (`:4190` in the final run, the same `server/serve-dist.mjs` as `:4180`): `/`, `/play`, `/tape/<any>` → 200 `text/html`; a missing asset → 404; `/../.keys/player-a.json` → 404; POST → 405 | **PASS** (final run, `:4190` serving `.localnet/dist-final`, bundle `entry-48a18129…`, 02:48 UTC) — `/`, `/play`, `/tape/anything` → 200 `text/html; charset=utf-8`; missing asset → 404; `/../.keys/player-a.json` → 404; `POST /` → 405 |
| A7 | fogduel program | `3K3v1bp6…Rj1` executable, with the current build's program data on chain *(plan corrected: the 798,056 bytes written before the crank and tape-window work are now 935,024)* | **PASS** (final run, 02:48 UTC) — `3K3v1bp6…Rj1` `Executable: true`, program data 935,024 bytes, last deployed in slot 61 of the ledger reset at 01:43, authority `3YUg…wsS5`; the same 935,024-byte build is also deployed on devnet (ProgramData `7mcpvZnc…`, slot 497500594, read back from `api.devnet.solana.com`) |
| A8 | Delegation, permission and session programs | `DELeGG…`, `ACLseo…`, `KeyspM2…` all executable | **PASS** (final run, 02:48 UTC) — `DELeGG…aeSh`, `ACLseo…Qnp1`, `KeyspM2…wde5` all `executable: true`, and so is the committor `ComtrB2…dABq` that `localnet.sh` now preloads |
| A9 | Export freshness | `npm run check:build` passes: the bundle names `127.0.0.1:8999`, `127.0.0.1:6699` and the program id, with no scratch port | **PASS** (final run, 02:49 UTC) — `npm run check:build`: 7 assertions — the export points at `http://127.0.0.1:8999` and `http://127.0.0.1:6699`, carries the deployed program id, no scratch-build override; `dist/` and the tested `.localnet/dist-final` are the same bundle (`entry-48a18129…`) |
| A10 | Script keypairs funded | After `localnet.sh`, `player-a` … `player-e` each hold ≥ 5 SOL | **PASS** (final run, 02:48 UTC) — player-a 19.92, player-b 22.96, player-c 20.07, player-d 19.73, player-e 19.88 SOL |

## B. Landing `/`

The landing was replaced by `src/landing` (another session's work). Items are
added from a line-level spec of that code before B is executed; B is
executed last among the routes.

## C. Lobby `/play`

| # | Item | Correct means | Status |
|---|---|---|---|
| C1 | Market list | MEMES lists ≥ 10 pump.fun markets, each with symbol, short mint, USD price and market cap | **FAIL → fixed → PASS** — final run on bundle `entry-7dbee6b1…`: 12 MEMES rows, every one with symbol, short mint (`VEY7…pump · WOFI`), USD price and cap; 38 proxy requests on reload all 200; 0 console errors. The first final-run pass FAILED it: the short mint showed only on rows whose ticker repeated. Fix: `src/ui/MarketRow.tsx` always shows `mint · name`, the mint lit yellow when the ticker is ambiguous |
| C2 | Prices are live | A row's price is within 2% of a direct `/pump/` fetch made at the same moment | **PASS** — final run: direct `frontend-api-v3.pump.fun/coins` fetch at 03:02:41 UTC vs the lobby at 03:02:53: VEY7 $1.6902 / $1.69, LVC9 $0.52000 / $0.5204, vbvt $0.48526 / $0.4856, 12Ra $0.086098 / $0.0861 — all 12 rows within 0.1%, same order |
| C3 | Logos | Every visible row shows its real logo or a letter tile; zero failed image requests; zero console errors | **PASS** — final bundle `entry-3299c973…` (11:06 UTC), `/play` on `localhost:4190`: MEMES lists 12 markets with 12 real logos loaded, 0 broken, 0 letter tiles; the load made 98 `/img/check` and `/img` relay requests and every one returned 200 (ipfs.io, arweave, irys, pbs.twimg and pump.fun image sources among them); 0 console errors. |
| C4 | MAJORS tab | Switches to Jupiter majors (SOL, USDC, …); the list changes; logos load | **PASS** — final bundle (11:06 UTC): MAJORS lists 40 Jupiter markets (SOL $99.69, tSAT, USDC, USDT, …): 38 real logos loaded plus SOL and USDC as bundled SVGs, 0 broken, 0 letter tiles; every relay request 200; 0 console errors. |
| C5 | Search by ticker | `bonk` → Bonk ranks first | **PASS** — final bundle `entry-7efb2b6f…`: `bonk` → Bonk (DezX…B263, $0.00000278) first, then BONK 6u8S…pump and bonkSOL; `/jup/tokens/v2/search?query=bonk` 200; 0 console errors |
| C6 | Search ranking | `btc` → WBTC above BTCBANK; `wif` → $WIF first | **PASS** — final bundle: `btc` → WBTC (3NZ9…qmJh, Wrapped BTC (Portal), $77,311) first of 17, BTCBANK 12th; `wif` → $WIF (EKpQ…zcjm, dogwifhat, $0.1898) first. Both ranked from the live verified and pump lists already loaded — the app asks Jupiter's search only below 8 local matches (`markets.ts:257`); 0 console errors |
| C7 | Search by whole mint | Pasting a real mint → that token first | **PASS** — final bundle: pasting `DezXAZ8z7PnrnRJjz3wXBoRgixCa6xjnB7YaB1pPB263` → exactly one row, Bonk (DezX…B263, $0.00000278); search request 200; 0 console errors |
| C8 | Search, valid mint that is not a token | `NOTHING MATCHING "<MINT>"`, no crash, no error | **PASS** — final bundle: the treasury address `DfvNVoN7…BDZ4` (a real account, not a token) → `NOTHING MATCHING "DFVNVON7…BDZ4"`, 0 rows; search request 200; no crash; 0 console errors |
| C9 | Stocks reachable | `TSLAx` → a "Tesla xStock" row with a live USD price | **PASS** — final bundle: `TSLAx` → `TSLAx XsDo…HzoB · Tesla xStock $367.33` first, a live Jupiter price, then pump.fun namesakes with their own mints; 0 console errors |
| C10 | Stake picker | 0.05 / 0.10 / 0.50 / 1◎ selectable; the note reads `WINNER TAKES` pot × 0.98 (0.05 → 0.098◎) · 2% rake | **PASS** — final local bundle `entry-b2081670…` (Bnyq, 5.19 SOL): 0.05◎ → `WINNER TAKES 0.098◎ · 2% RAKE`, 0.10◎ → 0.196◎, 0.50◎ → 0.98◎, 1◎ → 1.96◎; the chosen stake yellow (255,179,30), the rest blue; 0 console errors |
| C11 | Round length picker | 1 MIN / 5 MIN / 15 MIN; the selection highlights; notes "a sprint — one or two fills each" / "the product round" / "room for a thesis to play out" | **PASS** — final bundle: `1 MIN` → "a sprint — one or two fills each", `5 MIN` → "the product round", `15 MIN` → "room for a thesis to play out"; exactly the chosen length cyan (53,224,255); 0 console errors |
| C12 | CTA follows selection | A market selected → `FIND MATCH`; none → `PICK A MARKET`, disabled | **PASS** — final bundle: a market selected → `FIND MATCH`, enabled; nothing selected → `PICK A MARKET`, disabled — reached two ways: the market feed down (hero `PICK A MARKET · CHOOSE ONE BELOW` above `MARKET FEED DOWN — Could not reach the price service.` with `RETRY`), and a `?market=` link that cannot be priced (with the `NO PRICE FOR THAT TOKEN` notice); 0 console errors |
| C13 ⚡ | Market feed down | Proxy stopped → `MARKET FEED DOWN` and "Could not reach the price service." with RETRY; no prices; CTA `PICK A MARKET` disabled | **PASS** — final bundle `entry-3299c973…` (10:39 UTC): market proxy stopped at 10:38:32; a fresh `/play` on `localhost.:4190` → `MARKET FEED DOWN` / "Could not reach the price service." with `RETRY`; no prices listed; the CTA reads `PICK A MARKET` and is disabled; 0 console errors. |
| C14 ⚡ | RETRY recovers | Proxy restarted → RETRY repopulates the list; CTA returns to `FIND MATCH` | **PASS (recovery; the RETRY press itself was not isolated)** — final bundle `entry-3299c973…` (10:40–10:41 UTC): proxy restarted at 10:40:25; with no reload the same `/play` recovered — `MARKET FEED DOWN` and `RETRY` gone, 25 prices listed, market `WOFI · $1.39 · pump.fun`, CTA back to an enabled `FIND MATCH` — but the lobby's own refresh had repopulated the list by 10:40:33, before `RETRY` could be pressed, so the press was not observed on its own; 0 console errors. |
| C15 | Remembered choices | Pick 0.10◎, 1 MIN and a market, reload `/play` → the same three are selected | **PASS** — final local bundle: picked 0.10◎, `1 MIN` and WOTF (LVC9…pump) → stored `{stake: 0.1, duration: 60, marketMint: LVC9…}`; visited `/tape`, `/stats`, `/proof`, reloaded `/play` → 0.10◎, `1 MIN` and WOTF still selected; 0 console errors |
| C16 | Bad stored prefs | `masked.prefs.v1` = `{"stake":"NaN","duration":9999}` → ignored; defaults shown; no error | **PASS** — final local bundle: `masked.prefs.v1` = `{"stake":"NaN","duration":9999}` → reload → the defaults: 0.10◎, `5 MIN` and the top market (WOFI), no error boundary; the prefs are rewritten valid (`{stake: 0.1, duration: 300, marketMint: VEY7…}`); 0 console errors |
| C17 | `?market=<mint>` | Lobby opens with that market selected; an unpriceable mint → a `NO PRICE FOR THAT TOKEN` notice naming it, nothing selected (`PICK A MARKET`, disabled), the remembered pick kept, no error | **FAIL → fixed → PASS** — final bundle `entry-95ccde17…` (fresh `localhost.:4190`): `?market=DezX…B263` → Bonk selected (`$0.00000278 · jupiter`), no notice, `FIND MATCH`; `?market=DfvNVoN7…BDZ4` (a real account, not a token) → toast `NO PRICE FOR THAT TOKEN` / "DfvN…BDZ4 has no live price on pump.fun or Jupiter, so a duel cannot open on it. Pick a market from the list.", hero and CTA `PICK A MARKET` (disabled), and the remembered pick kept (`DezX…` before and after; a plain `/play` afterwards opens on Bonk); 0 console errors. Found in the final run: the lobby's top-market default silently stood in for the unpriceable link — WOFI selected, `FIND MATCH` live, nothing said — and the remembered pick was erased. Fixes: `useDuel.ts` tells the player (restore effect) and no longer writes an empty market over the remembered one (prefs effect); `DuelLobbyScreen.tsx`/`MaskedApp.tsx` (parallel session) skip the top-market default after a `?market=` link |

## D. Wallet

| # | Item | Correct means | Status |
|---|---|---|---|
| D1 | Disconnected | No stored key → header shows `CONNECT` | **PASS** — fresh origin `localhost.:4190` (no `masked.localKeypair.v1`; only `masked.prefs.v1`): header shows `CONNECT`, balance chip 0.00, trophies 0; 0 console errors |
| D2 | Picker | `CONNECT` → "CONNECT A WALLET" with `SOLFLARE`, `LOCAL KEY (DEV)`, `CANCEL` | **PASS** — `CONNECT` → dialog `CONNECT A WALLET` with exactly `SOLFLARE`, `LOCAL KEY (DEV)` and `CANCEL`; 0 console errors |
| D3 | Local key connect | Creates `masked.localKeypair.v1`; header shows the new pubkey (short form) | **PASS** — `LOCAL KEY (DEV)` → `masked.localKeypair.v1` written (a 64-byte key); the header shows `3ENt…ZFcn`, the public key derived from it (`3ENtrai26yyZfFH3vSJMZFNgnoJbRnacseiwsFzuZFcn`); `CONNECT` is gone |
| D4 | Balance is real | Header balance equals `getBalance` for that pubkey, to 2dp | **PASS** — header balance `0.00` = `getBalance(3ENt…ZFcn)` on :8999 = 0 SOL |
| D5 | Persistence | Reload → same pubkey | **PASS** — reload → the stored key still derives `3ENt…ZFcn`, and the header shows it |
| D6 | Cleared key | Clear the wallet's storage (key and remembered wallet) and reload → `CONNECT`; reconnect gives a different pubkey at 0.00; no crash. *(Clearing only the key reconnects with a fresh key instead — the local wallet's auto-connect, by design.)* | **PASS** — final bundle: clearing the key and the remembered wallet (`masked.localKeypair.v1`, `walletName`) and reloading → `CONNECT`, no crash; `LOCAL KEY (DEV)` → a new key `HjJA…URxP` (neither of the two before it) at `0.00` = its RPC balance; 0 console errors. Clearing the key alone while `walletName` still says Local Key reconnects straight away with a freshly minted key (`2k9x…msyw` at 0.00): the local key wallet auto-connects on purpose so a refresh mid-round returns to the round (`WalletProvider.tsx:43`), and its `connect` mints a key when none is stored |
| D7 | Zero-balance guard | 0 SOL → every stake disabled (`THIS WALLET CANNOT COVER A STAKE YET. FUND IT FIRST.`), the chosen stake named (`CHOSEN STAKE <stake> IS MORE THAN THIS WALLET COVERS`); `OPEN A MATCH` → exactly one toast `NOT ENOUGH SOL` / "Need ~<stake + 0.05> SOL, wallet holds 0.00. Fund this wallet first."; **no transaction** | **PASS** — final bundle `entry-3299c973…` (09:54 UTC), `localhost.:4190` wallet BTpQ at 1,000,000 lamports: every stake disabled with `THIS WALLET CANNOT COVER A STAKE YET. FUND IT FIRST.` and `CHOSEN STAKE 0.05◎ IS MORE THAN THIS WALLET COVERS`; `OPEN A MATCH` → exactly one toast `NOT ENOUGH SOL` / "Need ~0.10 SOL, wallet holds 0.00. Fund this wallet first."; no transaction (the wallet's newest signature is still its 09:27:34 one); 0 console errors. Headroom is now 0.05◎ (measured join + seal cost), so the figure is stake + 0.05. |
| D8 | Partial balance guard | 0.12 SOL with 0.10◎ as the saved stake → only 0.05◎ enabled (`BALANCE COVERS UP TO 0.05◎`), the chosen stake named; `OPEN A MATCH` → "Need ~0.15 SOL, wallet holds 0.12. The largest you can open right now is 0.05◎."; no transaction | **PASS** — final bundle `entry-3299c973…` (10:17 UTC), `localhost.:4190` wallet BTpQ funded to exactly 0.12 SOL (120,000,000 lamports), saved stake 0.10◎: only 0.05◎ enabled (0.10◎, 0.50◎, 1◎ disabled), `BALANCE COVERS UP TO 0.05◎` and `CHOSEN STAKE 0.10◎ IS MORE THAN THIS WALLET COVERS`; `OPEN A MATCH` → exactly one toast `NOT ENOUGH SOL` / "Need ~0.15 SOL, wallet holds 0.12. The largest you can open right now is 0.05◎."; no transaction (newest signature is the 10:17:05 funding transfer); 0 console errors. Re-cut for the 0.05◎ headroom: a 0.09 SOL wallet now covers no stake. |
| D9 | Solflare | Listed and selectable without an uncaught error. Completing a connect needs the Solflare extension and an account — **UNTESTED** if absent | **PASS (listed and selectable) · completing a connect UNTESTED — needs the Solflare extension and an account** — final bundle: `CONNECT` → `SOLFLARE` closes the picker, stores `walletName: "Solflare"` and loads Solflare's own connect frame from `connect.solflare.com`; 0 console errors. Reload → no frame, `CONNECT` again, because Solflare is deliberately left out of auto-connect (`WalletProvider.tsx:26`); 0 console errors |

## E. Matchmaking

| # | Item | Correct means | Status |
|---|---|---|---|
| E1 | FIND MATCH | Moves to `MATCHMAKING`: your mask, `WAITING…`, pot pill, `OPEN A MATCH`, `OPEN BOOK` | **PASS** — final bundle, creator `localhost:4190` (Bnyq): `FIND MATCH` → `MATCHMAKING · 1 VS 1` with the player's mask `(YOU)`, `WAITING…`, pot pill `POT 0.20◎ · WOFI`, `OPEN A MATCH` and `OPEN BOOK`; 0 console errors |
| E2 | OPEN A MATCH | Exactly one new `Match` on chain: creator = wallet, entry = stake, duration = picked length, leg A mint = chosen market; balance drops by entry + rent + fees | **PASS** — one click on `OPEN A MATCH` → exactly one new match `5NCVeHL4…g3an` on chain (open 1, cancelled 1, settled 3): creator Bnyq, entry 100,000,000 lamports (0.10◎), duration 300 s (5 MIN), leg A mint `VEY7…pump` (WOFI, priced through Jupiter because the lobby resolved it by search; the source label says so everywhere); balance 5,129,812,480 → 5,022,436,840 = entry + 7,370,640 rent + 5,000 fee |
| E3 | Own match in the book | Row reads `YOUR MATCH` with `CANCEL`; not joinable | **PASS** — final local bundle: 5t6Q's book row for its own match reads `WOFI · YOUR MATCH · 300s · 22s ago · 0.05◎ · CANCEL`; no `JOIN` anywhere in its book |
| E4 | Double-click OPEN | Exactly one new match on chain | **PASS** — double-click on `OPEN A MATCH` → exactly one new match `6Jn97Z91…9CMD` (the chain showed 1 open where there had been 0), the screen moved to its own-match state (`COPY INVITE LINK`, `CANCEL`); no toast; 0 console errors |
| E5 | CANCEL | Match status `cancelled` on chain; entry refunded (balance restored less fees and rent) | **PASS** — `6Jn97Z91…` status `cancelled` on chain; the creator's balance rose from 5,029,817,480 to 5,129,812,480 = the 0.10◎ entry less the 5,000-lamport cancel fee (the 7,370,640 rent of the record stays with it) |
| E6 | Double-click CANCEL | Exactly one refund; no error toast | **PASS** — double-click on `CANCEL` → one `MATCH CANCELLED` toast, no error; the match's transactions at `confirmed` are exactly `CreateMatch` (03:57:06) and one `CancelIfUnjoined` (03:58:34), both ok — no second or failed cancel reached the chain |
| E7 | JOIN from the book | The other origin joins → both clients go live; on chain `status=live`, joiner = B, leg B mint = B's market | **PASS** — final bundle `entry-3299c973…` (10:22 UTC): duel `FHF3qgxy…` (Bnyq opens 0.10◎, 5 MIN, SOL; 5t6Q joins from the book on PUMP): `JOIN` 10:22:44 → `MATCH JOINED` 10:22:48 → 5t6Q live with `SEALED · ACL ON CHAIN`; Bnyq's tab entered the same round with no reload (E14); on chain `status=live`, creator Bnyq, joiner 5t6Q, pot 0.2◎, leg B `pump…9Dfn` (the joiner's PUMP, not the creator's SOL); 0 console errors on both tabs. |
| E8 | Seal reads back | Both positions owned by `DELeGG…`; permission accounts exist; the live screen shows the sealed label | **PASS** — final bundle, duel `FHF3qgxy…` read on the base layer at 10:24:56 UTC: both positions owned by `DELeGG…` (559 bytes), both permission accounts `DELeGG…` (567 bytes), the round status `DELeGG…` (43 bytes); the joiner's live screen reads `SEALED · ACL ON CHAIN`, the creator's `SEALED`. |
| E9 | Stale match | A match older than 300 s shows `STALE` and cannot be joined | **PASS** — final local bundle, fixture `HX6WZMtD…` (opened by 5t6Q at 04:58:29): at 05:07:37 Bnyq's book reads `WOFI · 5t6Q…7oVb · 300s · 9m ago · 0.05◎ · STALE` beside the two older stale matches, and there is no `JOIN` anywhere; the match stayed `open` with no joiner until its owner cancelled it |
| E10 | Refresh mid-open | Reload right after OPEN → back in matchmaking with the one match; no duplicate on chain | **PASS** — reload right after `OPEN A MATCH` → back in `MATCHMAKING` with exactly one `WOFI · YOUR MATCH` row and `POT 0.10◎ · WOFI`; the chain still holds exactly one open match for the creator (no duplicate); 0 console errors |
| E11 | Own stale match | The creator's own row for a match past 300 s reads `STALE` beside `CANCEL` — added after E9 found it read only `CANCEL` | **FAIL → fixed → PASS** — final local bundle: at the same moment 5t6Q's own row reads `WOFI · YOUR MATCH · 300s · 9m ago · 0.05◎ · STALE · CANCEL`; `CANCEL` → `MATCH CANCELLED`, `cancelled` on chain. Found in E9 of this run: the row showed only `CANCEL`, so a creator waited on a match nobody could join. Fix: `src/ui/MatchRow.tsx` shows `STALE` beside `CANCEL` on the owner's own stale row |
| E12 | Book agrees with the toast | At the `MATCH OPEN` toast the own row and `COPY INVITE LINK` are already listed and the header balance has already dropped — no moment where `OPEN A MATCH` sits over a book without the match; at `MATCH CANCELLED` the row is already gone (no `CANCEL` left to press) and the balance is back less fees and rent — added when this run found both windows | **FAIL → fixed → PASS** — found on bundle `entry-7459da9e…` (09:04–09:13 UTC): at the `MATCH OPEN` toast the book had no own row and no `COPY INVITE LINK` while `OPEN A MATCH` stayed pressable (the row arrived 4,438 ms later); pressing it again 1.46 s after the toast really opened a second escrowed match (`JAYs5X…`, `Cjzk1j…`, both then cancelled); at `MATCH CANCELLED` the cancelled row still offered `CANCEL`, and pressing it toasted "MATCH NOT OPEN · Someone joined first." Cause: the book only polled (4 s; about a minute in a hidden tab) and nothing told it about this client's own open or cancel. Fix: `refreshOpenMatches()` in `src/chain/useOpenMatches.ts` (every mounted book re-reads now; an older poll can no longer overwrite a newer read), awaited in `src/screens/useDuel.ts` together with a balance re-read before `MATCH OPEN` and `MATCH CANCELLED`; `MatchNotOpen` now reads "Someone joined it or it was cancelled first." Final bundle `entry-3299c973…` (09:54 UTC, Bnyq): at the `MATCH OPEN` toast 1 own row and `COPY INVITE LINK`; 1.2 s later still 1 row (no second press possible over an empty book); at `MATCH CANCELLED`, and again after React's effects flushed, 0 own rows and no `CANCEL` to press; chain `2QMJFSHp…`: base 2 transactions, 0 failed (`create_match` 1, `cancel_if_unjoined` 1), rollup 0; 0 console errors. The pocket balance read 4.94 at the toast, 4.93 at +1.2 s and 4.89 at +12 s in a hidden tab (see note in the final report). |
| E13 | Joiner who cannot cover the seal | `JOIN` from a wallet holding the entry and the join's rent but not the seal → one `NOT ENOUGH SOL` toast before anything is sent; no `join_match` on chain; the match stays open — added when a real low-balance join lost its entry | **FAIL → fixed → PASS** — found on bundle `entry-7459da9e…` (09:26 UTC): BTpQ, funded to exactly entry + join rent + one permission (77,143,280 lamports), joined Bnyq's 0.05◎ match `As5sY6…`; `join_match` and one `create_position_permission` landed, the second permission ran out of rent, the retried press re-sent `join_match` and showed only `ALREADY EXISTS · That account has been created before.`; the screen stayed in matchmaking while Bnyq's waiter sealed the round and `settle_match` paid Bnyq +87,346,200 — the joiner lost the entry in a round their screen never showed. Fix: JOIN from the book now runs `checkBalance(balance, match.entry)` before anything is sent; `HEADROOM_LAMPORTS` 0.02 → 0.05◎ (measured: join 21,301,080 + seal 26,002,760 lamports); a retry resumes after a landed write instead of re-sending it (`joined` in `joinMatchByAddress`, `landed` in `openOrJoin`). Final bundle `entry-3299c973…` (10:22 UTC): BTpQ at 127,143,280 lamports (entry 0.10 + join + one permission) presses `JOIN` on Bnyq's 0.10◎ `FHF3qgxy…` → exactly one toast `NOT ENOUGH SOL` / "Need ~0.15 SOL, wallet holds 0.12. The largest you can open right now is 0.05◎."; stays in matchmaking; no transaction (its newest signature is the 10:19:10 top-up); the match stayed open and 5t6Q joined it 9 s later; 0 console errors. |
| E14 | Creator's older match taken | Creator opens A, then B; the other wallet joins A → the creator's tab enters A's round without a reload instead of waiting on B — added when code review found the watch followed only the newest match | **FAIL (found in code review) → fixed → PASS** — the creator's watch followed only the newest match it had opened, so a creator whose older open match was taken sat in matchmaking while that round ran and settled without them (found reading the code; not separately reproduced on the old bundle). Fix: the waiter in `src/screens/useDuel.ts` also asks `fetchMyLiveMatch` every 10 s, timed rather than counted so a throttled hidden tab still checks. Final bundle `entry-3299c973…`: Bnyq opened A `FHF3qgxy…` (10:19:56) then B `945thsNV…` (10:20:06), so the watch was on B; 5t6Q joined A at 10:22:48; Bnyq's hidden tab, never reloaded (the load-time resume had already latched), was still in matchmaking at 10:23:24 and on A's live screen (`ENDING IN`, `SEALED`, LONG/SHORT) by 10:24:35 — the same catch-up a hidden creator tab shows on the ordinary path; B stayed open; 0 console errors. |

## F. Live round

| # | Item | Correct means | Status |
|---|---|---|---|
| F1 | Clock | `ENDING IN` counts down from the picked duration on both clients, within 2 s of each other | **UNTESTED as specified — needs both players' clients in visible, unthrottled browser windows.** Every Chrome window on this machine sat behind other apps (`document.visibilityState` hidden), so Chrome throttled the tabs' timers: at 05:21:51.8 UTC the popup client read `00:01:26` — within 1 s of the chain (buzzer 05:23:19) — while the background tab read `00:01:44` (its text last repainted ~17 s earlier). After a reload that same tab read `00:02:54` with 173 s left on chain, and the spectator tab read `0:03` with 5 s left: each client counts from the chain's own `start_ts` |
| F2 | Match-found card | `TRADE NOW (4)` counting down, `<n> MIN ROUND · LIVE NOW`; names both wallets and tickers; dismisses on click or after 4 s; shows once | **PASS** — `MATCH FOUND · 5 MIN ROUND · LIVE NOW`, `5t6Quc… (YOU) VS Bnyqfb…`, `$WOTF $WOFI`, `GET READY — Both players are locked in. Your positions are sealed from each other until the buzzer.`, `TRADE NOW (3)` counting down at +6 s, gone by +13 s |
| F3 | Opponent fogged in UI | Opponent PnL `FOGGED`, side `SEALED`; no size, price or fill count anywhere; the opponent's ticker is shown | **PASS** — the opponent row reads `BNYQFB… FOGGED $WOFI SEALED` with an empty PnL bar; the only line about them is `THEIR FILLS HIDDEN`; no size, price or fill count of theirs anywhere in the page text; their ticker shown |
| F4 | Fog enforced on the wire | Reading the opponent's position through the gate from the page, with this wallet's token, returns no data | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q), 05:21:51 UTC: from Bnyq's page, a real Ed25519 login at the gate (`/auth/challenge` → WebCrypto signature → `/auth/login` → token), then `getAccountInfo` through `127.0.0.1:6699`: the opponent's position `HWPyUbaq…` → no data with Bnyq's token and no data anonymously; Bnyq's own position `2wtpKi5v…` → 559 bytes (the control) |
| F5 | Session key | Fills are signed and paid by the session key, not the wallet (checked on the rollup transaction) | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): the four `ApplyFill` transactions on 5t6Q's position (10:24:36, 10:24:49, 10:26:31, 10:27:00) are paid and signed by the session key `6J3h…`, not the wallet; the live screen reads `SESSION KEY · NO SIGNATURE PER FILL`. |
| F6 | Size picker | 1/4, 1/2 and MAX each change the quoted impact % | **PASS** — `1/4` → `THAT SIZE COSTS 0.39% IN IMPACT`, `1/2` → `LONGING COSTS 0.73%`, `MAX` → `1.56%`; each size changes the quote |
| F7 | LONG | Fill lands on the rollup; `YOUR FILLS` +1; realised `exec/mark − 1` equals the quoted impact to 2dp | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): at 1/4 the quote read `THAT SIZE COSTS 0.39% IN IMPACT`; `LONG` → `LONG FILLED` 90 ms later with `BOUGHT ON YOUR BOOK · -0.39% · mark was 0.000037354◎ · you filled at 0.000037500◎` (exec/mark − 1 = 0.39%, the quote to 2 dp); the fill is `ApplyFill` on the rollup at 10:24:36, 0 failed; side chip `LONG`. |
| F8 | Double-click LONG | Exactly one fill | **PASS** — LONG pressed twice in immediate succession (focus + Enter ×2) → exactly one new fill (`LONG 0.005454679`); the rollup shows exactly two buys in total |
| F9 | SHORT from flat | On-chain `base_qty` < 0; side chip `▼ SHORT` | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q): from `FLAT`, `SHORT` → `SHORT FROM 0.017952725`, `SOLD ON YOUR BOOK -0.38%`, tape `SHORT 0.017952725`; on chain the first fill is a sell taking `base_qty` to −1,366,494 |
| F10 | CLOSE | Closes a long by selling and a short by buying → flat on chain; CLOSE while flat says so and sends no transaction | **FAIL → fixed → PASS** — the FAIL, in duel 2 `HZ6mpkBd…`: a MAX close of a 1,024,870 short bought 1,024,871 (a float inverse rounded up) and left the player one unit long, a FLIP on the tape. Fix: `exactQuoteToCover` (src/chain/book.ts) inverts the curve in the program's own integer arithmetic — the largest spend that stops at zero, exact from 0.001 ◎/token up; below that (PUMP, tSAT, fresh memes, where a lamport buys more than one base unit) no spend lands exactly and it leaves under a lamport, which `isFlat` (src/chain/units.ts) reads as flat on the round's readouts and at CLOSE; `check-fuzz` holds all three claims exactly, in bigint, against `Book::buy`. Re-verified in duel 3 `HzfxoKXz…` (bundle entry-46711f49; Bnyq on WOFI, 5t6Q on PUMP; 0.10◎, 5 MIN; settled 06:03:12): both MAX SHORT 05:59:39, both MAX CLOSE 06:00:32 → `BOUGHT ON YOUR BOOK`, `POSITION CLOSED`, card `FLAT`, tape `COVER` (no FLIP); CLOSE again → WOFI `You are flat — there is no position to close.`, PUMP `You are flat — what is left is worth under a lamport, and the buzzer closes it.`, no transaction. On chain: WOFI `sell→-5,364,845 buy→0` (exactly flat before the buzzer, no SETTLE fill); PUMP `sell→-2,609,291,043 buy→-15`, then the buzzer's `settle→0` closing those 15 base (0.567 lamports at the final mark) for 0 — checked by `check-tape` |
| F11 | Nothing left to long | LONG with the whole entry in the position → `NOTHING LEFT TO LONG`; no transaction | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q): `MAX` + `LONG` spent all quote (`BOUGHT ON YOUR BOOK -1.56%`, quote 0 on chain); `LONG` again → `NOTHING LEFT TO LONG`, and the fill count stayed at 4 |
| F12 | Keyboard | `L` and `C` do what LONG and CLOSE do | **PASS** — keyboard `L` bought (F7) and `C` sold a quarter of the open long (`CLOSE 0.005438116`, `SOLD ON YOUR BOOK -0.17%`), exactly what the LONG and CLOSE buttons do at that size |
| F13 | Settle button mid-round | Disabled, reads `SETTLES AT THE BUZZER` | **PASS** — `SETTLES AT THE BUZZER`, `aria-disabled=true`, throughout the live round |
| F14 | Mark crank | Price feed `px` advances on the base layer about every 5 s; the direction caret appears after the first change; no `MARK POSTED TOO SOON` toast | **PASS** — caret now checked, final bundle `entry-3299c973…` (1 MIN round opened by Bnyq at 11:01:42): on 5t6Q's live screen the direction caret is drawn beside `MARK`; when the mark moved from 0.000037359◎ to 0.000037240◎ the caret turned red (`#ff4d5e`, down); no `MARK POSTED TOO SOON` toast in any of today's rounds. Cadence: about every 5 s from a visible client; hidden tabs stretch it (round `8v7z2Rbu…` feed gaps up to 59 s), and because both clients push the same feed, 2–3 pushes a round fail with `PriceTooSoon` in the background (no toast). |
| F15 | HUD and arena | Balance real; trophies = on-chain wins; axis symmetric about 0; puck shows live PnL; potential earnings = pot × 0.98; standings show `#?` for both | **PASS** — balance chip 4.50 = `getBalance(5t6Q…)` 4.4984 SOL; trophies: Bnyq's header `Trophies 4` and 5t6Q's `0` = their on-chain `PlayerStats.wins` (4 and 0); axis symmetric (±0.0200%, later ±0.090%); the puck sits on the live PnL (-0.090%); potential earnings `+0.196◎` = pot 0.20 × 0.98; both standings `#?` |
| F16 | `aria-live` | Exactly two polite live regions: clock and PnL | **PASS** — exactly two `aria-live=polite` regions while live: the clock (`00:03:05`) and `YOUR PNL +0.00%`; none on the reveal |
| F17 | `beforeunload` | Prevented while live; not prevented in the lobby or on the reveal | **PASS** — a `beforeunload` dispatched while live is prevented; on the reveal and on the lobby's feed tab it is not |

## G. Settlement and reveal

| # | Item | Correct means | Status |
|---|---|---|---|
| G1 | Buzzer | At 0:00 settlement starts with no user action | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): nothing was pressed on either tab at or after 0:00; the rollup's crank committed at 10:27:45, `SettleMatch` landed at 10:28:39 from Bnyq's client, and both reveals were up by 10:29:18. |
| G2 | Settle stages | commit → undelegate → settle, each reporting a real result | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q): Bnyq's client logged `COMMIT waiting for the rollup's crank · UNDELEGATE Solana takes ownership back from the delegation program · SETTLE PnL compared, pot paid, tape written` at 05:23:33, then `COMMIT committed by the rollup's own crank · UNDELEGATE both positions back under the program` at 05:23:34, then the reveal; the chain: `CrankCommitRound` 05:23:19, `ProcessUndelegation` 05:23:18, `SettleMatch` 05:23:33 |
| G3 | Undelegated before settle | Both positions owned by fogduel again before `settle_match` | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): each position's base history is `JoinMatch` → `CreatePositionPermission` → `DelegatePositionPermission` → `DelegatePositionToEr` (10:22:43–46, sealed by the joiner) → `ProcessUndelegation` 10:27:45 (`mAGi…`) → `SettleMatch` 10:28:39; after settlement both positions are owned by fogduel `3K3v1b…` again; 0 failed. |
| G4 | Both reach the reveal | Both clients show the reveal | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): both hidden tabs reached the reveal with no action after the buzzer — Bnyq `YOU TOOK THE POT`, 5t6Q `OOF… SO CLOSE`; 0 console errors on either. |
| G5 | Reveal mirrors | A's "you" equals B's "opponent" in bps, both ways | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): Bnyq's reveal `#1 Bnyqfb… (YOU) +0.0000% · #2 5T6QUC… −0.6500%`, 5t6Q's `#1 BNYQFB… +0.0000% · #2 5t6Quc… (YOU) −0.6500%` — each side's "you" is the other's "opponent" to the basis point. |
| G6 | PnL matches chain | Displayed % equals the tape's `pnlABps` / `pnlBBps` | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): tape `pnlABps 0`, `pnlBBps −65`; both reveals show +0.0000% and −0.6500%. |
| G7 | Timeline is real | Every mark is a fill on the tape; nothing synthesized | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle): the reveal's timeline is one SVG: two equity curves, four fill markers and one buzzer square on the loser's lane — exactly the tape's `sell, buy, buy, buy, settle` for Bnyq — and no markers on 5t6Q's lane, which never traded; `RoundTimeline` draws only points replayed from the tape's fills |
| G8 | Pot maths | `potPaid + rake = pot`; `TAKES` = pot × 0.98 | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): tape `potPaid 196,000,000` + `rake 4,000,000` = pot 200,000,000; the winner's reveal reads `+0.196◎` (0.2 × 0.98), the loser's `POT LOST −0.10◎`. |
| G9 | Result board | Ordered by the chain's `winner`; trophy on #1; headline `YOU TOOK THE POT` or `OOF… SO CLOSE` with the gap | **PASS** — final bundle, Bnyq's reveal of the 1 MIN round opened at 11:01:42 (11:04 UTC): rows ordered by the chain's winner, `#1` Bnyq with the headline `YOU TOOK THE POT`; the `#1` row's icon is the same SVG as the header's trophy counter, and differs from the `#2` row's (the last-place skull in `RankRow.tsx`). On `FHF3qgxy…` the loser's board read `OOF… SO CLOSE · You missed the top by 0.6500% (round PnL)`; 0 console errors. |
| G10 | Win burst | Fires once on a win, never on a loss | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q): a mutation observer in each client from 05:23:02: the winner's client (5t6Q) mounted the burst exactly once (28 pixels at 05:23:36.614); the loser's (Bnyq) never did |
| G11 | Settle race | Both clients settling at once → no `MATCH NOT LIVE` dead end | **PASS** — both clients settled at the buzzer without a `MATCH NOT LIVE` dead end: exactly one `SettleMatch`, 0 failed transactions, both reveals |
| G12 | The rollup commits the round | At 0:00 the commit stage reads `committed by the rollup's own crank`; the positions' commits are rollup `Executed crank` transactions, not client ones | **PASS** — final bundle: at the buzzer of the 1 MIN round opened at 11:01:42, Bnyq's settle panel read `SETTLING ON SOLANA › COMMIT waiting for the rollup's crank · UNDELEGATE … · SETTLE …` (11:03:39.536), then `committed by the rollup's own crank` (11:03:39.625), then `pot paid, tape written` (11:03:40.348). On chain (duel `FHF3qgxy…`) both positions' commits are `CrankCommitRound` from the rollup's crank signer `mAGi…` (10:22:48, 10:27:45), with no client-sent commit. |
| G13 | ACLs come home | After settlement both players' permission accounts are owned by `ACLseo…` on base again | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): after settlement both permission accounts are owned by `ACLseo…` (567 bytes) on the base layer again. |
| G14 | No failed transactions | Every base-layer and rollup transaction touching the match, both positions and the status carries no `err` | **PASS** — duel `FHF3qgxy…` (final bundle `entry-3299c973…`, 10:22–10:29 UTC): base — each position 6 transactions, 0 failed; status 4, 0 failed. Rollup — position A 153 (147 `CrankLiquidate`), position B 157, 0 failed. Outside this row's accounts, 5t6Q's price feed shows 3 failed `push_price` of 15 (see F14). |

## H. Reveal actions

| # | Item | Correct means | Status |
|---|---|---|---|
| H1 | REMATCH | Starts a fresh match flow; never resurrects the old match | **PASS** — final bundle (10:31 UTC), Bnyq on the reveal of `FHF3qgxy…`: `REMATCH` → `MATCHMAKING` with the stake (0.10◎) and market (SOL) carried over (`POT 0.20◎ · SOL`); the settled match was not resurrected (the book held only Bnyq's older open `945thsNV…`); `OPEN A MATCH` then opened a fresh match `ED6X…` at 10:33:43, which 5t6Q joined and both played. REMATCH as built lands in matchmaking without opening; `FADE WINNER` is the action that opens on its own (`MaskedApp.tsx`). The comment above `rematch` in `useDuel.ts` still claims it opens the match — out of date, noted. |
| H2 | Share | Copies `/tape/<match>` (`LINK COPIED`) or says `COPY BLOCKED`; never a false success | **PASS** — `COPY TAPE LINK` → the button reads `LINK COPIED` (clipboard allowed in this browser) |
| H3 | SEE IT IN THE FEED | Lands on the feed with this duel present | **PASS** — `SEE IT IN THE FEED` → the feed's `REVEALS` with this duel first: `FOG DUEL · WOFI · Bnyq…Co4r +0.00% · POT 0.20◎ · 5t6Q…7oVb -0.15%` |
| H4 | 375 px | All three actions reachable | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q): the winner's reveal in a 375 × 760 window: `REMATCH` (48–185 px), `COPY TAPE LINK` (191–327), `SAVE RESULT IMAGE` (48–327) and `SEE IT IN THE FEED` (48–327) each scroll fully into the viewport; no horizontal overflow |

## I. Feed, Rank, Modes, Quests

| # | Item | Correct means | Status |
|---|---|---|---|
| I1 | FEED REVEALS | Row count equals the settled tapes on chain | **PASS** — `REVEALS` pages ten at a time: `SHOW 10 MORE · 10 OF 30`; the chain holds 30 tapes |
| I2 | FEED MINE | Only this wallet's duels; with none → `YOU HAVE NOT SETTLED A DUEL YET` | **PASS** — `MINE` (5t6Q): `4 SETTLED DUELS WITH THIS WALLET · NEWEST FIRST`, all four with 5t6Q, matching its 4 tapes (`r6Md` -15, `DVWJ` 0, `92M7` 0, `Eifd` -30 bps) |
| I3 | FEED BIG POTS | Largest pots, ordered | **PASS** — `BIG POTS` (potPaid ≥ 0.2◎): the 1◎ and 0.5◎ pots, largest first — exactly the chain's two tapes above the threshold |
| I4 | Feed row data | Pubkeys, PnL and pot equal the tape | **PASS** — MINE's four cards: pubkeys, PnL and pot equal each tape (`Bnyq +0.00% · POT 0.20◎ · 5t6Q -0.15%` = `r6Md` 0 / -15 bps, pot 0.20; `-0.30%` = `Eifd` -30 bps) |
| I5 | Card actions | `READ TAPE` → `/tape/<match>`; `DUEL THIS TOKEN` → lobby with that mint selected | **PASS** — `READ TAPE` → `/tape/r6MdMukgsC3M5agBHtaeM5hFTJRMqW1BZjueFxcWdKB`; back; `DUEL THIS TOKEN` → the lobby with WOFI (`VEY7…pump`) selected |
| I6 | RANK | Players from on-chain stats; tier chips from wins | **PASS** — `FOG WINS · ALL TIME · 28 PLAYERS · BEST STREAK 4` = 28 `PlayerStats` accounts, best streak 4; ordered by SOL taken (3YUg 1.96◎, 2cPZ 0.98◎, 8WZZ 0.784◎, Bnyq 0.588◎ …); wins and tier chips from wins (4W `SILVER`, 1W and 0W `BRONZE`) |
| I7 | MODES | FOG DUEL `LIVE`; 11 tiles `SOON`, none playable | **PASS** — `FOG DUEL` `LIVE`; 11 tiles `SOON` (CHICKEN … THESIS FIGHT); no enabled control on any of them |
| I8 | QUESTS | Achievements, all time, CLEARED/LOCKED from real stats | **PASS** — 5t6Q: `1/4 CLEARED · ALL TIME · read from your on-chain record`: `Settle your first duel` CLEARED 1/1, `Win 3 fog duels` LOCKED 0/3, `Reach a 2-win streak` LOCKED 0/2, `Take 1◎ from the fog` LOCKED 0.00/1◎ — its record: 4 settled, 0 wins, 0 taken |

## J. `/tape`

| # | Item | Correct means | Status |
|---|---|---|---|
| J1 | Index | Explains or lists; no crash with no argument | **PASS** — final local bundle: `/tape` → `ALL TAPES · 30 SETTLED DUELS · NEWEST FIRST` with each duel's markets, players, PnL and payout (`WOFI vs WOTF · 45m ago · Bnyq…Co4r +0.00% · paid 0.20◎ · 5t6Q…7oVb -0.15%` first); 30 = the tapes on chain; 0 console errors |
| J2 | Settled duel | Every fill of both players: side, size, exec price, second | **PASS** — `/tape/r6MdMu…dKB`: `5t6Q…7oVb — EVERY FILL`: `02:00 BUY 4.58 @ 0.005455697◎`, `03:34 BUY 3.44`, `03:41 SELL 2.00`, `03:47 SELL 1.50`, `03:49 SELL 3.42`, `05:03 BUZZER 1.09 @ 0.005443136◎` — side, size, exec price and second, matching the tape account (base 4,582,366 → 8,019,782 → 6,014,837 → 4,511,128 → 1,093,110 → 0); `Bnyq…Co4r — EVERY FILL · never traded` |
| J3 | Replay | Replayed PnL equals the chain's bps | **PASS** — the replay ends on `Bnyq…Co4r +0.00%` and `5t6Q…7oVb -0.15%` = the tape's `pnlABps 0` and `pnlBBps -15` |
| J4 | Payout line | `POT x◎ · TAKES y◎`, rake shown, and they add up | **PASS** — `POT 0.20◎ · TAKES 0.196◎` and `paid to winner 0.196◎ · rake 0.004◎ · settled 2026-09-13 04:12:41 UTC`: 0.196 + 0.004 = 0.20 |
| J5 | Malformed address | "That is not a match address." | **PASS** — `/tape/not-a-key` → `NOTHING TO READ · That is not a match address.`; 0 console errors |
| J6 | Valid key, not a duel | "That address is a Solana account, but not a duel." | **PASS** — `/tape/DfvNVoN7…BDZ4` (the treasury, a real account) → `That address is a Solana account, but not a duel.` |
| J7 | Unsettled duel | "That duel has not settled yet. Watch it at /spectate instead." | **PASS** — `/tape/FNsiN64x…gNoL` (a live match past its buzzer, unsettled) → `That duel has not settled yet. Watch it at /spectate instead.` |
| J8 | No wallet | Works disconnected | **PASS** — `localhost.:4190` with no wallet selected (`walletName` removed): `/tape/r6MdMu…` renders every fill and `paid to winner 0.196◎ rake 0.004◎`; 0 console errors |
| J9 | Busy duel's tape | A side with more than 16 fills: the replay starts from the tape's recorded window start and lands on the chain's PnL, and the page says it shows the last 16 of N — added after `check:tape` caught the opposite | **PASS** — final local bundle: `/tape/JCw3xFAz…` (38 fills on one side) → `BD9J…AGAV · LAST 16 OF 38 FILLS`, sixteen entries under `Bd9j…agAV — LAST 16 OF 38 FILLS`, `WOFI MOVED +0.11% OVER ITS LAST 16 OF 38 FILLS`, and the replay lands on `-2.08%` = the tape's `pnlBBps -208`; `/tape/5mxUqV8K…` (25 a side) → `LAST 16 OF 25 FILLS` on both sides, `-0.01%` each = `-1` / `-1` bps; 0 console errors |
| J10 | Share on X | `SHARE ON X` opens X's post composer (`x.com/intent/post`) carrying this tape's own `/tape/<match>` link and the result, in a new tab; nothing is posted by the app — added when the parallel session shipped the button | **FAIL → fixed → PASS** — final local bundle: `SHARE ON X` opens `x.com/intent/post` in a new tab with "WOFI +0.00% vs WOTF -0.15%, settled on MASKED. Private during the fight, public after." and `http://127.0.0.1:8791/t/r6MdMu…`; following that link → the share page (200 HTML, og/twitter title "WOFI +0.00% vs WOTF -0.15% · MASKED", an 8 KB PNG card) → redirect → `localhost:4190/tape/r6MdMu…` rendering the settled tape; 0 console errors. Found in this run: on a local run the share page read devnet tapes (404 for every local duel) and sent visitors to the hosted devnet app. Fix (parallel session, `server/src/tapes.ts` + `server/market-proxy.mjs`): `TAPES_L1_URL` and new `TAPES_APP_URL` default to the local stack unless the proxy is hosted (`PORT` set); the local proxy restarted on it with `TAPES_APP_URL=http://localhost:4190` for this static export |

## K. `/spectate`

| # | Item | Correct means | Status |
|---|---|---|---|
| K1 | Index | No crash with no argument | **PASS** — final local bundle, no wallet: `/spectate` → `LIVE DUELS · 0 DUELS RUNNING · Nothing running right now. A duel opened at /play shows up here while its clock runs; finished ones are at /tape.`; no crash; 0 console errors |
| K2 | Live duel | Pot and both marks; each player `FOGGED`; `BOTH POSITIONS SEALED`; neither position's contents anywhere in the DOM. *(Corrected before running: the plan asked for fill counts, but a live duel's counts live in the sealed positions — `SpectateScreen` shows them only once `revealed` at settlement, which K3 checks)* | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q), 05:23:14: `/spectate/HZ6mpkBd…` with no position data: `POT 0.20◎ · TAKES 0.196◎`, both marks (`WOFI 0.018045761◎` ×2), clock `0:03` (5 s left on chain), `Bnyq…Co4r FOGGED`, `5t6Q…7oVb FOGGED`, `BOTH POSITIONS SEALED`; Bnyq's base (5,442,283) and no position field anywhere in the page; 0 console errors |
| K3 | Settled duel | Settled result and payout | **PASS** — `/spectate/r6MdMu…` → `POT 0.20◎ · TAKES 0.196◎`, `Bnyq…Co4r +0.00% 0 fills`, `5t6Q…7oVb -0.15% 6 fills`, `SETTLED ON SOLANA · paid to winner 0.196◎ · rake 0.004◎` = the tape |
| K4 | Malformed address | "That is not a match address." | **PASS** — `/spectate/not-a-key` → `NOTHING TO WATCH · That is not a match address.` |
| K5 | Valid key, not a duel | "That address is a Solana account, but not a duel." | **PASS** — `/spectate/DfvNVoN7…BDZ4` → `That address is a Solana account, but not a duel.` |
| K6 | No wallet | Works disconnected | **PASS** — K1, K3–K5 all ran on `localhost.:4190` with no wallet selected (`walletName` null); 0 console errors |

## L. `/proof`

| # | Item | Correct means | Status |
|---|---|---|---|
| L1 | Cluster rows | name `LOCAL`, base `http://127.0.0.1:8999`, rollup `http://127.0.0.1:6699`, validator `mAGicP…1mev`, read gate `YES — sealed position refused, control served`, gate attested `NO — not a TEE` | **PASS** — final local bundle `entry-b2081670…`, `localhost.:4190` with no wallet selected: `NO TEE`; name `LOCAL`, base `http://127.0.0.1:8999`, rollup `http://127.0.0.1:6699`, validator `mAGicP…1mev`, read gate `YES — sealed position refused, control served`, gate attested `NO — not a TEE` |
| L2 | Measured speed | Real numbers that change between two loads | **PASS** — two loads: base 1.8 slots/s, rollup 20.0 slots/s, round trips 20.8 ms / 23.1 ms, then 1.8, 20.2, 18.4 ms / 18.5 ms — measured, and different between loads |
| L3 | Programs on chain | fogduel, delegation and permission rows with real addresses | **PASS** — fogduel `3K3v1b…1Rj1 · 36B`, delegation `DELeGG…aeSh · 36B`, permission `ACLseo…Qnp1 · 36B` (the upgradeable program accounts) |
| L4 | Settled on chain | `duels settled` equals the tape count | **PASS** — `duels settled 30` = the 30 tapes on chain; `paid out 6.272 SOL` = 98% of the 6.4◎ in pots (the summed rake is 0.128◎); `open matches 3` = the chain's open matches |
| L5 | Live right now | Live duels link to `/spectate/<match>`; expired-but-unsettled counted separately | **PASS** — `LIVE RIGHT NOW 0 DUELS · Nothing running` with none running on chain, and separately `18 more duels are over but unsettled` = the chain's 18 live matches past their buzzer |
| L6 | Access control lists | The latest *sealed* duel's permission accounts. After settlement both read `HOME ON SOLANA` (owner `ACLseo…`): each player's client releases its own at the buzzer. Expectation changed 00:40 UTC — before that, ACLs stayed owned by `DELeGG…` forever | **PASS** — the latest sealed duel (`r6MdMu…`): winner position ACL `Hz6y1b…sS7a · HOME ON SOLANA`, loser position ACL `Ai2QPm…Wtyh · HOME ON SOLANA`, matching the chain (both owned by `ACLseo…`) |
| L7 | Life of one duel | Real base-layer rows with slots and signatures; a row opens the explorer | **PASS** — `THE LIFE OF ONE DUEL 12` for `r6MdMukg…`: `JOIN MATCH slot 16029` … `DELEGATE POSITION TO ER slot 16034/16035` … `ACL BACK ON SOLANA slot 16606` ×2 … `PROCESS UNDELEGATION slot 16610` ×2 … `SETTLE MATCH slot 16613`, each with its signature; tapping a row opens `explorer.solana.com/tx/211KFish…` with `cluster=custom` and `customUrl` 127.0.0.1:8999, in a new tab |
| L8 | What one duel costs | Fees summed from those transactions | **PASS** — `WHAT ONE DUEL COSTS 12 TX · total network fees 0.001500 SOL · per transaction 0.000125 SOL · refused on chain none · against the pot 0.750%` = the chain: those 12 transactions' fees are 4 × 365,000 + 8 × 5,000 = 1,500,000 lamports |
| L9 | Recent program transactions | Real signatures and labels | **PASS** — `RECENT PROGRAM TRANSACTIONS 10`: `CREATE MATCH oNgUvSsD… 7m`, `CANCEL IF UNJOINED 5oF4pfbS… 8m`, `CREATE MATCH 5Hh9KxUe… 13m`, `CANCEL IF UNJOINED 43pfG7Zp… 43m`, `SETTLE MATCH 3JYmfYar… 53m`, `REQUEST SETTLE`, `PROCESS UNDELEGATION` ×3, `PUSH PRICE` — this run's own fixtures and duel, with real signatures |
| L10 | Run this yourself | Tap → `COPIED`, or `COPY BLOCKED` | **PASS** — tapping `npm run check:gate · TAP TO COPY` in a window without focus → `COPY BLOCKED` (the clipboard refused; no false `COPIED`); with focus the same control said `LINK COPIED` in H2 |
| L11 | No wallet | Whole page works disconnected | **PASS** — final local bundle `entry-b2081670…`, `localhost.:4190` with no wallet selected: the whole page renders every section above; 0 console errors |
| L12 | `npm run crank` | /proof's claim holds: every joined match past its buzzer is committed, undelegated and settled by a third party, with no player online; the expired count drops to 0 | **PASS** — `npm run crank` (cranker `3YUg…`, 05:09:55–05:10:29 UTC): `18 match(es) past the buzzer`, all 18 `settled` with winner and pot; the chain afterwards: 0 live matches, 48 tapes, treasury less rent 168,000,000 lamports = the summed rake |

## M. `/health`

| # | Item | Correct means | Status |
|---|---|---|---|
| M1 | Dependencies | Every row UP with a real detail | **PASS** — final bundle `/health`: 8 dependency rows UP, each with live detail: base layer slot 13,942; ephemeral rollup slot 150,110; fogduel program 36 bytes (the upgradeable program account); delegation and permission programs deployed; market proxy `http://127.0.0.1:8791`; pump.fun listing markets; Jupiter SOL $101.66; build 1.0.0 · LOCAL · tee NO; 0 console errors |
| M2 | Badge | `ALL SYSTEMS UP` only when every row is up | **PASS** — final bundle: with every dependency up the badge reads `ALL SYSTEMS UP`; with the market proxy stopped (03:49:49 UTC), `RE-CHECK` turns it to `DEGRADED` and exactly the three market rows to `DOWN — did not answer` (market proxy, pump.fun, Jupiter), while base layer, rollup and the three programs stay up with live slots (14,093 / 151,716) |
| M3 ⚡ | Proxy down | That row DOWN — and the market rows that are fetched through it; badge `DEGRADED`; the chain rows still UP | **PASS** — final bundle `entry-3299c973…` (10:39:55 UTC), `/health` with the proxy stopped: badge `DEGRADED`; `market proxy · DOWN — did not answer`, with `pump.fun` and `jupiter` DOWN as well — correctly, since the app reaches both only through the proxy's `/pump/` and `/jup/`; base layer (slot 51209), ephemeral rollup (slot 629751), fogduel (935,024 bytes), delegation and permission programs all UP; no spinner; 0 console errors. |
| M4 | RE-CHECK | Re-runs and recovers after the proxy returns | **PASS** — final bundle `entry-3299c973…` (10:41:54 UTC): after the proxy restart `RE-CHECK` → `ALL SYSTEMS UP`, proxy `http://127.0.0.1:8791`, pump.fun `listing markets`, Jupiter `SOL $99.62`; 0 console errors. |
| M5 ⚡ | Gate down | The rollup row DOWN, reported as unreachable rather than empty; no permanent spinner | **PASS** — final bundle (10:36 UTC), `/health` on `localhost.:4190` with the read gate stopped: `RE-CHECK` → badge `DEGRADED`, `ephemeral rollup · DOWN — did not answer` (reported as unreachable, not as an empty result), every other row still up (base layer slot 50758, fogduel 935,024 bytes deployed, delegation and permission programs, market proxy, pump.fun, Jupiter SOL $99.77); no spinner left on screen; before the stop the same page read `ALL SYSTEMS UP`; 0 console errors. |

## N. `/stats`

| # | Item | Correct means | Status |
|---|---|---|---|
| N1 | Loads with no wallet | Every figure present and non-negative; `DUELS SETTLED` = tape count | **PASS** — final local bundle, `localhost.:4190` with no wallet selected: `DUELS SETTLED 30` (= 30 tapes), `PAID TO WINNERS 6.272◎`, `PLAYERS 28` (= 28 `PlayerStats`), `FILLS RECORDED 224`, `OPEN NOW 2` (= the chain's open matches after the fixtures were cancelled), `LIVE NOW 0 · 18 awaiting settlement` (= 18 expired live matches); every figure present and non-negative; 0 console errors |
| N2 | Rake, two sources | `TREASURY, LESS RENT` = `SUMMED FROM TAPES` to the lamport; badge `AGREES TO THE LAMPORT` | **PASS** — `SUMMED FROM TAPES 0.128◎` and `TREASURY, LESS RENT 0.128◎` (`holds 0.129◎, 0.001◎ locked as rent`), badge `AGREES TO THE LAMPORT` — the chain: treasury 128,953,520 lamports less rent 953,520 = 128,000,000 = the rake summed over 30 tapes |
| N3 | Rent floor | The locked figure equals the treasury's real rent-exempt minimum | **PASS** — the locked figure `0.001◎` is the treasury's real rent-exempt minimum for its 9 bytes, 953,520 lamports (0.00095352◎), shown to three places |
| N4 | Per-market table | One row per leg mint, busiest first, with duels / volume / biggest pot | **PASS** — one row per leg mint, busiest first, with duels, volume, biggest pot and the mint (`VEY7…pump`, `Bygc…pump`, …), so the repeated WOFI tickers are told apart |
| N5 ⚡ | Cluster unreachable | `COULD NOT REACH THE CLUSTER — THESE ARE NOT ZEROS, THEY ARE UNKNOWN` | **PASS (on screen; the console did not stay clean)** — final bundle `entry-3299c973…` (10:47 UTC): base validator stopped cleanly at 10:47:11; a fresh `/stats` on `localhost.:4190` → `COULD NOT REACH THE CLUSTER — THESE ARE NOT ZEROS, THEY ARE UNKNOWN`, and no figures drawn. But while the base layer was down, the console logged `ws error:` about every 1.5 s (9 in 15 s) — web3.js's own websocket reconnect loop, printed from inside the bundle, not app code — so this outage is the one surface where console errors appear. Known, not fixed before the deadline. The base layer was restarted on the same ledger right after. |

## O. Static routes

| # | Item | Correct means | Status |
|---|---|---|---|
| O1 | `/gallery` | Component showcase whose invented sample figures are visibly labelled as sample data | **PASS** — `/gallery` → `SAMPLE DATA — COMPONENT GALLERY · Every figure on this page is a frozen or generated example for showing components, not live chain state. The real duel is at /play.`; 0 console errors |
| O2 | 404 | Unknown route → branded 404 linking all 8 routes (`/`, `/play`, `/proof`, `/stats`, `/health`, `/spectate`, `/tape`, `/gallery`); each link navigates | **PASS** — `/definitely-not-a-route` → `404 · NO SUCH ROUTE · That page does not exist. Every route this app has is below.` with exactly eight links; clicking each lands on its route and renders it: `/play`, `/proof`, `/stats`, `/health`, `/spectate`, `/tape`, `/gallery`, `/` (`LOCAL CLUSTER LIVE · 2 OPEN ROOMS`); 0 console errors |
| O3 | Browser back | Back out of a flow lands on a valid screen | **PASS** — back after each of those eight navigations returns to the rendered 404 page (never a blank or broken screen); 0 console errors |

## P. Market proxy API

| # | Item | Correct means | Status |
|---|---|---|---|
| P1 | `/pump/` | 200 JSON, real pump.fun coins | **PASS** (final run, 02:58 UTC) — `GET /pump/coins?offset=0&limit=3&sort=market_cap&order=DESC&includeNsfw=false` → 200 `application/json`, three real pump.fun coins (`WOFI` `VEY71U…`, `WOFI` `AACMeD…`, `WOFI` `baeHvY…`) |
| P2 | `/jup/` | 200 JSON, real Jupiter tokens | **PASS** (final run, 02:58 UTC) — `GET /jup/tokens/v2/search?query=bonk` → 200 `application/json`, 20 real Jupiter tokens led by `Bonk` `DezXAZ…` |
| P3 | Unknown path | 403 `{"error":"not a market route", "allowed": [...]}` | **PASS** (final run, 02:58 UTC) — `GET /definitely/not/a/route` → 403 `{"error":"not a market route","allowed":["/pump/","/jup/"]}` |
| P4 | `/img` relays a logo | 200 `image/*` with `cross-origin-resource-policy: cross-origin` | **PASS** (final run, 02:58 UTC) — `GET /img?url=https://static.jup.ag/jup/icon.png` → 200 `image/png` with `cross-origin-resource-policy: cross-origin` |
| P5 | `/img` private addresses | `localhost`, `127.0.0.1`, `169.254.169.254` → 403 | **PASS** (final run, 02:58 UTC) — `https://localhost/…` → 403 `localhost resolves to a private address`; `https://127.0.0.1/…` → 403 `127.0.0.1 resolves to a private address`; `https://169.254.169.254/latest/meta-data` → 403 `169.254.169.254 resolves to a private address`; `http://localhost/…` → 403 `https only` |
| P6 | `/img` over http | Refused: `{"error":"https only"}` | **PASS** (final run, 02:58 UTC) — `GET /img?url=http://static.jup.ag/jup/icon.png` → 403 `{"error":"https only"}` |
| P7 | `/img` non-image | 415, not relayed | **PASS** (final run, 02:58 UTC) — `GET /img?url=https://api.jup.ag/price/v3?ids=So111…` → 415 `{"error":"not an image: application/json"}`, nothing relayed |
| P8 | No secrets | No key or token in any client request URL or body | **PASS** — the shipped bundle (`entry-48a18129…`, 2.8 MB) has no secret-shaped strings (`sk-…`, `ghp_…`, `AKIA…`, PEM private keys, `api_key=` literals) and no embedded 64-byte key arrays; the market proxy reads only `MARKET_PROXY_PORT` and sends no upstream credentials (`/whoami` names its upstreams, nothing more). Every client request captured in this pass carried public data only: JSON-RPC calls, public mints and search terms to the proxy, image URLs to its relay; the rollup gate's token rides in an `Authorization` header, and its challenge URL carries only the public key |

## Q. On-chain program — real signed transactions

| # | Item | Correct means | Status |
|---|---|---|---|
| Q1 | `create_match` | Escrow held; match on chain (E2) | **PASS** — UI duel `B1NxXkHD…`: `CreateMatch` ok; the match holds the creator's 1◎ in escrow, pot 2◎ after the join (chain) |
| Q2 | `join_match` | Second escrow; both permissions created and delegated (E7, E8) | **PASS** — `JoinMatch` ok; second escrow; both permissions created and delegated exactly once (E7, E8) |
| Q3 | `cancel_if_unjoined` | Refunds; refused once joined (`check:guards`) | **PASS** — `check:guards` in the parallel session's green 22-step `npm run check` (00:20:56 UTC): refunds an unjoined match, refuses cancel-after-join |
| Q4 | `push_price` limits | Rate limit and 5% jump cap enforced (`check:guards`) | **PASS** — `check:guards` in the parallel session's green 22-step `npm run check` (00:20:56 UTC), and the anchor test 'refuses a price push that jumps further than the rate limit' |
| Q5 | `apply_fill` | Owner enforced; session-signed fill accepted (`check:session`) | **PASS** — `check:session` in the parallel session's green 22-step `npm run check` (00:20:56 UTC); F5: the UI fill signed by the session key alone |
| Q6 | Settlement | Pot paid, rake taken, tape written (G) | **PASS** — `B1NxXkHD…`: winner paid 1.96◎, rake taken, 1157-byte tape written; `check:invariants` 665 assertions |
| Q7 | Delegation lifecycle | Fill signature in the rollup ledger, absent from base (`check:er`) | **PASS** — `check:er` in the parallel session's green 22-step `npm run check` (00:20:56 UTC); the UI duel's `ApplyFill` exists only in the rollup ledger |
| Q8 | Gate enforces ACL | Sealed REFUSED, bare SERVED, token opens own position only (`check:gate`) | **PASS** — `check:gate` in the parallel session's green 22-step `npm run check` (00:20:56 UTC); `/proof` probes it live: `YES — sealed position refused, control served` |
| Q9 | Guard refusals | Every refusal the app can hit is enforced (`check:guards`) | **PASS** — `check:guards` in the parallel session's green 22-step `npm run check` (00:20:56 UTC) (7 refusals, each enforced by the deployed program) |
| Q10 | Shorts and liquidation | Real short, margin cap refused, real liquidation (`check:short`) | **PASS** — `check:short` in the parallel session's green 22-step `npm run check` (00:20:56 UTC) (a real short, the margin cap refused, a real liquidation); and `tests/er-cranks.ts`: the rollup's keeper liquidates with nobody calling `liquidate` |
| Q11 | VRF request | Request lands on chain (`check:vrf`); **fulfilment UNTESTED — no oracle on the local stack** | **PASS** — the request lands on both clusters (the local VRF program accepts it; no oracle serves the local queue), and on devnet today (12:47 UTC) the oracle fulfilled it: `check:vrf` → `VRF OK — the market was chosen on chain by the oracle, not by the client`. No game mode is built on the draw yet (BLIND DRAFT is a SOON tile). |
| Q12 | Full suite | `npm run check` green, all 22 steps (lint added as step 2) | **PASS** — re-run on the final source, 11:12:24–11:16:34 UTC, against the live local stack: `npm run check` exit 0, all 22 steps — typecheck clean; lint 0 errors (16 warnings); build (7: export points at the local cluster, deployed program id); tokens (61); series (30); fog (14); errors (41); preflight (18, live cluster); markets; pumpfun; tape (1,892 assertions over 57 real tapes); fuzz (65,764 over 4,000 randomised books); invariants (688 over 57 settled tapes: pot conserved, rake exact, winner matches the program); h2h (129); race (24: two real clients sealed and settled one match concurrently); guards (8 refusals enforced by the deployed program); session (15: a real session token signing a real rollup fill); short (23: a real negative position, margin cap and liquidation); legs (10); er (7: fill executed on the rollup, never seen by the base layer); sealed; gate. Log: `.localnet/npm-check-final.log`. |
| Q13 | Anchor tests | 38 passing, including the rollup crank suites in `tests/er-cranks.ts`; the 2 TEE tests **UNTESTED — no devnet SOL** | **PASS** — re-run against the deployed program on the final stack, 11:17–11:22 UTC: `anchor test --skip-local-validator --skip-build --skip-deploy` → 33 passing, 2 pending (the TEE-only pair), 1 failing — the `fogduel · permission ACL` suite's `before all` hook timed out confirming a transaction after 30 s while another session's disk cleanup loaded the machine, which skipped that suite's 5 tests. Re-run alone at 12:45 UTC (`mocha tests/permission.ts` against `http://127.0.0.1:8999`): 5 passing in 4 s, including the ER-visibility report (public gate: own and opponent positions absent, price feed readable). Together: 38 passing, 2 pending, as documented. |
| Q14 | Busy round | A round with 20+ fills per position comes home at the buzzer and settles — added after the soak found the opposite | **FAIL → fixed → PASS** — found by the parallel session's soak at 00:50 UTC: after ~21 fills a position's commit no longer fits one transaction, the rollup stages it through MagicBlock's committor program `ComtrB2K…`, and `mb-test-validator` 0.14.10 does not preload that program — the base transactions failed `ProgramAccountNotFound` and the intent stayed `Pending` for good (three rounds stranded: positions delegated, match `live`). Fixed in `scripts/localnet.sh` (preloads the program, warns on a ledger without it); stack reset from genesis at 01:11 UTC, old ledger kept at `.localnet/ledger-no-committor-20260913T011122Z` (deleted 2026-09-13 20:06 UTC in a disk cleanup, with the owner's OK). New suite `a busy round still comes home` in `tests/er-cranks.ts` green at 01:16 on the reset stack: 24 fills per side, the crank's commit brought both positions and the status home, both ACLs released, settled; the rollup's committor DB records both positions `DiffBuffer` `Succeeded`, and base shows the committor's `Init`/`Write`/`Close`, all ok Also the parallel session's soak on the fixed stack at 01:23 UTC: `CBJPzsRa…` (crank commit) and `J7EBtmgp…` (client commit), 42/42 fills each (p50 8 ms, p99 19 ms), both came home and settled; every committor row since 01:22:30 `Succeeded`, 0 retries. `check:race` (`HQ6DAvei…`, 24 assertions) and `check:short` (`57qPKfdh…`, 23) now end with each player releasing their own ACL, both home |

## R. Resilience

| # | Item | Correct means | Status |
|---|---|---|---|
| R1 | Refresh mid-round | Rejoins the live round with the clock correct | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q): Bnyq's tab reloaded at 05:20:10 mid-round → back in the live round at 05:20:24 with the clock `00:02:54` (chain: 173 s left), `SEALED` and its 4 fills; and the reload fix: that client asked the rollup for the round cranks again at 05:20:16 (`ScheduleRoundCranks` paid by Bnyq on the rollup) and sent no liquidation probes of its own (the rollup shows only the crank's `CrankLiquidate`) |
| R2 | Refresh mid-transaction | True state recovered; no double spend | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q): `SHORT` pressed and the tab reloaded 150 ms later → after the reload the tape shows the same 4 fills as the chain (`fill_count 4`), and nothing was spent twice |
| R3 | Back mid-flow | No broken state | **PASS** — duel 2 `HZ6mpkBd…` (final local bundle; Bnyq in a tab, 5t6Q in a 375 px popup window; 0.10◎, 5 MIN; settled 05:23:33, winner 5t6Q): the header `Back` mid-round → the feed tab, no dialog and no broken state; `DUEL` → the same live round (`LONG FROM 0.018339631`, `SEALED`) |
| R4 ⚡ | Gate unreachable mid-round | A fill fails with `CANNOT REACH THE ROLLUP`; never a false success; round state intact | **PASS** — final bundle `entry-3299c973…`, round 2 `ED6X…` live (10:35 UTC): the read gate was stopped at 10:35:10 (pid 14416, `:6699` refusing); 5t6Q pressed `LONG` at 10:35:46 → exactly one error, `CANNOT REACH THE ROLLUP` / "The ephemeral rollup is not answering. See /health.", at 10:35:48; no `LONG FILLED`, the position stayed `FLAT`, the tape still read `no fills yet`, and nothing had changed 18 s later (no false success); the round kept running; 0 console errors. The gate was restarted right after (see R5). |
| R5 | Gate restored | The next fill succeeds without a reload | **PASS** — final bundle (10:26 UTC), duel `FHF3qgxy…`: the read gate was stopped at 10:25:55 and restarted at 10:26:17 (new pid 14416, the flags from `scripts/localnet.sh`); 5t6Q's next `LONG` at 10:26:31 → `BUY SENT · CONFIRMING` → `LONG FILLED` (`LONG FROM 0.000037490`), and another at 10:27:00 filled as well — no reload; no request URL carried `token=`; 0 console errors. |
| R6 | Two tabs, same wallet | Same clock and position; no double fill or double settle on chain | **PASS** — final bundle `entry-3299c973…`, round `8v7z2Rbu…` (10:50–10:56 UTC): Bnyq's round on two tabs of the same origin — the first entered through its watch, the second resumed on load — both showed wallet `Bnyq…Co4r` and the same position: `FLAT`, then `LONG FROM 1.0039` on both after one press in the first tab, with the second tab raising no fill of its own. Same buzzer: the second tab's clock matched the chain's 10:55:42 end, while the first, hidden tab's display lagged (browser throttling). At 0:00 both tabs showed `SETTLING ON SOLANA › COMMIT waiting for the rollup's crank` and reached the reveal with no `MATCH NOT LIVE` or other error toast. On chain, Bnyq's position has exactly one `ApplyFill` (10:52:48, session key `AfVo…`) and the match exactly one `SettleMatch` (10:55:39), 0 failed on both positions and the status; 0 console errors on both tabs. |
| R7 | Two origins isolated | Separate wallets; no shared state | **PASS** — duel 3: `localhost:4190` holds Bnyq and `127.0.0.1:4190` holds 5t6Q, each with its own balance (4.94 and 4.38 ◎ after entry), its own market pick (WOFI and PUMP — choosing PUMP on one origin left the other on WOFI) and its own session key (rollup fills signed by 8Ko2 and DAQB) |
| R8 | Lobby sweep | Settles only this player's own abandoned rounds | **PASS (Phase 4 on bundle `entry-7459da9e…`; not re-run on the final bundle)** — a real abandoned round was swept by its own player's lobby and no one else's; the detailed evidence log lived in /private/tmp and was lost when the machine rebooted; the final bundle did not change the sweep. |
| R9 | Player leaves mid-round | Their mark keeps moving until the buzzer — a player who hides or closes the tab cannot freeze the price their PnL settles at — added after sampling found the opposite | **PASS** — duel `r6MdMu…dKB` (Bnyq opens, 5t6Q joins from the book; 0.10◎, 5 MIN; WOFI via Jupiter vs WOTF via pump.fun): the creator's tab sat hidden in the background (Chrome throttled it to roughly one wake-up a minute), and its WOFI feed still moved to the buzzer — 18 pushes 04:07:43–04:12:32, some signed by the joiner's client (the stale-mark backstop), gaps ≤ 42 s |

## S. Cross-cutting

| # | Item | Correct means | Status |
|---|---|---|---|
| S1 | 375 px | No horizontal overflow on any route; every control reachable | **PASS** — final bundle `entry-3299c973…` (10:36–10:42 UTC), each route in same-origin 375 px frames: `/`, `/play`, `/proof`, `/stats`, `/health`, `/spectate`, `/tape`, `/gallery` and a 404 all at document width 375 (no horizontal overflow) with every visible control inside the viewport or a horizontal scroller (0 outside on every route); 0 console errors. `/stats` has no controls by design (a no-wallet page). |
| S2 | 768 px | Same | **PASS** — final bundle `entry-3299c973…` (10:36–10:42 UTC), same probe in 768 px frames: `/`, `/play`, `/proof`, `/stats`, `/health`, `/spectate`, `/tape`, `/gallery` and a 404 at document width 768, 0 controls outside the viewport on every route; 0 console errors. |
| S3 | Desktop | Same | **PASS** — final bundle `entry-3299c973…` (10:36–10:42 UTC), same probe in 1440 px frames: `/`, `/play`, `/proof`, `/stats`, `/health`, `/spectate`, `/tape`, `/gallery` and a 404 at document width 1440, 0 controls outside on every route (`/` has 36 at this width); 0 console errors. |
| S4 | No stray logging | Only the ErrorBoundary's intentional `console.error` exists in shipped code | **PASS** — code sweep of `app/` and `src/` at 01:50 UTC: four console calls. Two are the intended ones — `ErrorBoundary.tsx:31` and `RouteErrorBoundary.tsx:26`, both `console.error` on a render error. The other two (`series.ts:91`, `PixelText.tsx:48`) are `console.warn` inside `if (__DEV__ && …)`, so a production export never runs them; every page driven in this pass logged 0 console errors |
| S5 | No mocks or stubs | No fabricated data on any product surface | **PASS** — code sweep for mock/fake/stub/lorem/`Math.random` in `app/` and `src/`: the only hit is `/gallery`'s `ROLL PNL` demo button, on the page bannered `SAMPLE DATA — COMPONENT GALLERY` (O1). Every product surface read in this pass (lobby, live round, reveal, `/proof`, `/stats`, `/tape`, `/spectate`, `/health`) showed values read back from the chain or the market proxy |
| S6 | Readable errors | No raw Anchor or fetch strings reachable by a user | **PASS** — final bundle `entry-3299c973…`: every failure path exercised today read as a sentence, never a raw error — `CANNOT REACH THE ROLLUP` / "The ephemeral rollup is not answering. See /health." (R4), `MARKET FEED DOWN` / "Could not reach the price service." (C13), `DOWN — did not answer` (M3, M5), `NOT ENOUGH SOL` with both amounts and the next move (D7, D8, E13); no Anchor error, program log, `Failed to fetch` or stack text reached a screen; 0 console errors. Paths not exercised today are covered by `check:errors` (41 assertions), re-run on the final source. |

## U. Untestable here — named dependencies

| # | Item | Missing dependency |
|---|---|---|
| U1 | TEE attestation, devnet deploy, the two TEE chain tests | **PASS** — verified today on the real devnet (12:47–12:54 UTC): `npx tsx scripts/check-tee.mts` → `devnet-tee.magicblock.app` answered a fresh 64-byte challenge with an Intel TDX quote that verified against Intel's collateral; the program is deployed on devnet at `3K3v1bp6…` (`solana program show`: 975,024 bytes, slot 497,557,294); `FOGDUEL_ER=tee mocha tests/er-privacy.ts` against devnet → 7 passing, including the two TEE-only PHASE 3 tests (a delegated ephemeral fee payer; both positions sealed private on the ER); `prove:privacy` → the opponent's position REFUSED mid-round with or without a signed token while the owner read their own; `verify:client` → CLIENT OK, a full match through `src/chain/client.ts` on the TEE (client PnL and chain both 656 bps, payout 0.196 SOL). |
| U2 | VRF fulfilment | **PASS** — verified today on devnet (12:47 UTC): `EXPO_PUBLIC_CLUSTER=devnet npm run check:vrf` → `VRF OK — the market was chosen on chain by the oracle, not by the client`. |
| U3 | Completing a Solflare connect | **UNTESTED** — needs the Solflare browser extension and a funded Solflare account; listing and selecting Solflare is verified (D9). |
