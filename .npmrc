# react-native-get-random-values@2 declares a react-native >=0.81 peer; the app is on Expo 52 / RN 0.76,
# where its web3.js polyfill works. Without this a clean `npm ci` stops at ERESOLVE.
legacy-peer-deps=true
